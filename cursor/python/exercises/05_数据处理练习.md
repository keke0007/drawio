# Python 数据处理练习

## 练习1：NumPy基础

### 任务
1. 创建NumPy数组（一维、二维、三维）
2. 数组操作：
   - 数组索引和切片
   - 数组形状变换
   - 数组运算（加减乘除）
   - 统计函数（mean, max, min, std）
3. 从 `data/numbers.txt` 读取数据创建数组
4. 生成随机数组并处理

### 要求
- 安装NumPy: `pip install numpy`
- 掌握基本数组操作
- 进行数学运算

---

## 练习2：Pandas基础

### 任务
1. 创建Series和DataFrame
2. 从 `data/sales.csv` 读取数据
3. 数据操作：
   - 查看数据（head, tail, info, describe）
   - 选择数据（列、行、条件筛选）
   - 添加和删除列
   - 数据排序
   - 数据分组和聚合

### 要求
- 安装Pandas: `pip install pandas`
- 掌握DataFrame操作
- 数据筛选和聚合

---

## 练习3：数据清洗

### 任务
使用 `data/dirty_data.csv` 进行数据清洗：
1. 处理缺失值：
   - 检测缺失值
   - 填充缺失值（均值、中位数、前向填充）
   - 删除缺失值
2. 处理重复数据：
   - 检测重复行
   - 删除重复行
3. 数据类型转换
4. 异常值处理

### 要求
- 完整的数据清洗流程
- 保存清洗后的数据

---

## 练习4：数据筛选和查询

### 任务
从 `data/employees.csv` 进行数据查询：
1. 条件筛选：
   - 工资大于5000的员工
   - 特定部门的员工
   - 多条件组合
2. 数据排序：
   - 按工资排序
   - 多列排序
3. 数据分组：
   - 按部门分组
   - 计算每组的统计信息

### 要求
- 使用布尔索引
- 使用groupby
- 生成统计报告

---

## 练习5：数据合并

### 任务
1. 从多个CSV文件读取数据：
   - `data/orders.csv`
   - `data/customers.csv`
   - `data/products.csv`
2. 数据合并：
   - 内连接（inner join）
   - 左连接（left join）
   - 外连接（outer join）
   - 合并多个DataFrame
3. 数据连接：
   - 按ID连接
   - 多键连接

### 要求
- 使用 `merge()` 和 `concat()`
- 理解不同连接类型
- 处理连接键

---

## 练习6：数据聚合和透视

### 任务
从 `data/sales_data.csv` 进行数据分析：
1. 分组聚合：
   - 按产品分组，计算总销售额
   - 按日期分组，计算每日销售额
   - 多级分组
2. 数据透视：
   - 创建透视表
   - 交叉表分析
3. 时间序列分析：
   - 按月份统计
   - 计算增长率

### 要求
- 使用 `groupby()` 和 `pivot_table()`
- 时间序列处理
- 生成分析报告

---

## 练习7：数据可视化

### 任务
使用Matplotlib和Seaborn可视化数据：
1. 从 `data/sales_data.csv` 读取数据
2. 创建图表：
   - 折线图（销售额趋势）
   - 柱状图（产品销量对比）
   - 饼图（市场份额）
   - 散点图（相关性分析）
   - 箱线图（数据分布）
3. 图表美化：
   - 添加标题和标签
   - 设置颜色和样式
   - 保存图表

### 要求
- 安装Matplotlib和Seaborn
- 创建多种图表类型
- 图表导出

---

## 练习8：时间序列分析

### 任务
1. 从 `data/time_series.csv` 读取时间序列数据
2. 时间处理：
   - 解析日期时间
   - 设置时间索引
   - 时间范围选择
3. 时间序列操作：
   - 重采样（日、周、月）
   - 移动平均
   - 时间序列统计
4. 可视化时间序列

### 要求
- 使用Pandas时间功能
- 时间序列分析
- 趋势分析

---

## 练习9：数据导出

### 任务
1. 从多个数据源读取数据
2. 数据处理和分析
3. 导出到不同格式：
   - CSV文件
   - Excel文件
   - JSON文件
   - HTML表格
4. 格式化导出：
   - 添加样式（Excel）
   - 格式化数字
   - 添加图表

### 要求
- 多种格式导出
- 格式化输出
- 批量处理

---

## 练习10：综合项目：销售数据分析系统

### 任务
创建一个完整的销售数据分析系统：
1. 数据加载：
   - 从 `data/sales.csv` 读取销售数据
   - 从 `data/products.csv` 读取产品数据
   - 从 `data/customers.csv` 读取客户数据
2. 数据清洗和预处理
3. 数据分析：
   - 总销售额统计
   - 产品销售排行
   - 客户分析
   - 时间趋势分析
   - 地域分析
4. 数据可视化：
   - 创建多个图表
   - 生成仪表板
5. 生成分析报告：
   - 文本报告
   - Excel报告（带图表）

### 要求
- 完整的数据分析流程
- 多种分析方法
- 可视化展示
- 报告生成
