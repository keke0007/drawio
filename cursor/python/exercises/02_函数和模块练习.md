# Python 函数和模块练习

## 练习1：函数基础

### 任务
创建以下函数：
1. `calculate_area(radius)` - 计算圆的面积
2. `calculate_perimeter(length, width)` - 计算矩形周长
3. `is_prime(n)` - 判断是否为质数
4. `factorial(n)` - 计算阶乘
5. `fibonacci(n)` - 生成斐波那契数列前n项

### 要求
- 每个函数都要有文档字符串
- 添加参数验证
- 测试所有函数

---

## 练习2：参数类型

### 任务
创建函数演示不同参数类型：
1. 位置参数
2. 关键字参数
3. 默认参数
4. 可变位置参数 (`*args`)
5. 可变关键字参数 (`**kwargs`)

### 示例
```python
def student_info(name, age=18, *courses, **scores):
    """演示各种参数类型"""
    pass
```

### 要求
- 创建综合示例函数
- 演示所有参数类型的用法

---

## 练习3：lambda函数

### 任务
1. 使用lambda函数实现：
   - 平方函数
   - 两数相加
   - 判断奇偶数
2. 与 `map()`, `filter()`, `reduce()` 结合使用
3. 从 `data/numbers.txt` 读取数据，使用lambda进行转换

### 要求
- 使用lambda替代普通函数
- 使用高阶函数

---

## 练习4：装饰器

### 任务
创建装饰器：
1. `@timer` - 计算函数执行时间
2. `@logger` - 记录函数调用日志
3. `@validate` - 验证函数参数
4. 组合使用多个装饰器

### 示例
```python
@timer
@logger
def expensive_function(n):
    # 耗时操作
    pass
```

### 要求
- 实现装饰器功能
- 保存日志到文件

---

## 练习5：作用域和闭包

### 任务
1. 演示局部变量和全局变量
2. 创建闭包函数（计数器）
3. 使用 `global` 和 `nonlocal` 关键字
4. 实现一个累加器函数

### 要求
- 理解变量作用域
- 实现闭包

---

## 练习6：模块导入

### 任务
1. 创建自定义模块 `my_math.py`，包含数学函数
2. 创建 `my_string.py`，包含字符串处理函数
3. 在主程序中导入并使用这些模块
4. 使用 `from ... import ...` 导入特定函数
5. 使用 `import as` 重命名模块

### 要求
- 创建多个模块文件
- 演示不同的导入方式

---

## 练习7：标准库使用

### 任务
使用标准库完成以下任务：
1. **os模块**: 遍历目录，列出所有文件
2. **sys模块**: 获取命令行参数
3. **datetime模块**: 处理日期时间
4. **random模块**: 生成随机数据
5. **math模块**: 数学计算
6. **collections模块**: 使用 `Counter`, `defaultdict`, `deque`

### 要求
- 使用多个标准库模块
- 完成实际任务

---

## 练习8：包的使用

### 任务
1. 创建一个包结构：
   ```
   mypackage/
       __init__.py
       utils.py
       data_processor.py
   ```
2. 在包中实现工具函数
3. 在主程序中导入包并使用

### 要求
- 创建包结构
- 实现包的功能

---

## 练习9：文件处理模块

### 任务
创建文件处理工具模块：
1. `read_file(filename)` - 读取文件
2. `write_file(filename, content)` - 写入文件
3. `copy_file(src, dst)` - 复制文件
4. `process_csv(filename)` - 处理CSV文件
5. `process_json(filename)` - 处理JSON文件

### 要求
- 使用 `csv` 和 `json` 模块
- 添加错误处理
- 处理 `data/` 目录下的文件

---

## 练习10：综合项目

### 任务
创建一个数据处理工具包：
1. 创建 `data_utils` 包
2. 实现以下模块：
   - `file_handler.py` - 文件操作
   - `data_cleaner.py` - 数据清洗
   - `data_analyzer.py` - 数据分析
   - `data_visualizer.py` - 数据可视化（可选）
3. 使用 `data/` 目录下的数据文件
4. 创建主程序使用这些工具

### 要求
- 完整的包结构
- 模块化设计
- 文档字符串
- 错误处理
